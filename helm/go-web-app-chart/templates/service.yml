apiVersion: v1
kind: Service
metadata:
  name: go-web-app
spec:
  selector:
    app: go-web-app
  type: ClusterIP
  ports:
  - port: 80          # Service port (Ingress connects here)
    targetPort: 8080
